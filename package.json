{
  "scripts": {
    "test:chrome": "playwright test --browser=chromium",
    "test:firefox": "playwright test --browser=firefox",
    "test:safari": "playwright test --browser=webkit"
  },
  "dependencies": {
    "@playwright/test": "^1.51.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10"
  }
}
